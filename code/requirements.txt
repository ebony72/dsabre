# Pinned to the versions used for every number in the paper.
# Measured single-threaded on Apple M2 / 16 GB / macOS 26.5, Python 3.13.

qiskit==2.3.0
networkx==3.5
numpy

# Cross-model comparison only (Section IV-E and Appendix D).
# pytket-dqc 0.0.1 is not on PyPI; install from
#   https://github.com/CQCL/pytket-dqc
# KaHyPar must be built with the same version its wheel expects.
pytket==2.16.0
kahypar==1.3.5
